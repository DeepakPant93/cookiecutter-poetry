codecov:
    notify:
        require_ci_to_pass: yes
coverage:
    range: 70..100
    round: down
    precision: 1
    status:
        project:
            default:
                target: 90%
                threshold: 0.5%
comment:
    layout: "header, diff, files"
    behavior: default
